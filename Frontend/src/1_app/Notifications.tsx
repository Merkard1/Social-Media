import { io } from "socket.io-client";

import { Text } from "@/6_shared/ui/Text/Text";

export const Notifications = () => {
  const hbhsd = 1;
  console.log(io);
  return (
    // eslint-disable-next-line i18next/no-literal-string
    <Text title="dmflkfkld" />
  );
};
